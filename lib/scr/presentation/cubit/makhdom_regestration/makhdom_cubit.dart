import 'package:flutter/material.dart';

class MakhdomLogin extends StatelessWidget {
  const MakhdomLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
